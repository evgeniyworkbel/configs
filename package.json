{
  "name": "configs",
  "version": "1.0.0",
  "description": "My own configs",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "npm publish --workspaces --access public"
  },
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
